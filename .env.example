# Directory used by Eywa for runtime artifacts (handoffs and index).
# Default: ~/.eywa
EYWA_DATA_DIR=~/.eywa

# Claude Code sessions directory (where session JSONL files live).
# Default: ~/.claude/projects
EYWA_SESSIONS_DIR=~/.claude/projects

# Claude task artifacts directory used for PID-based session detection.
# Default: <EYWA_SESSIONS_DIR parent>/tasks
EYWA_TASKS_DIR=~/.claude/tasks

# Model used by extractor sub-process (Node Claude Agent SDK).
# Default: sonnet
EYWA_CLAUDE_MODEL=sonnet

# Model used by eywa-batch through OpenRouter.
# Default: google/gemini-3-flash-preview
EYWA_OPENROUTER_MODEL=google/gemini-3-flash-preview

# Delay in seconds between batch API calls.
# Default: 0.5
EYWA_BATCH_DELAY=0.5

# Concurrent workers for batch session processing.
# Default: 5
EYWA_BATCH_CONCURRENCY=5

# OpenRouter API key (required for eywa-batch).
OPENROUTER_API_KEY=

# Timezone used when rendering session timestamps.
# Default: UTC
EYWA_TIMEZONE=UTC

# Optional logging level: DEBUG, INFO, WARNING, ERROR
# Default: INFO
EYWA_LOG_LEVEL=INFO
